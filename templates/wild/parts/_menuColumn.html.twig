<div class="col-12 col-sm-3 col-md-3 col-lg-2 sidebar py-2 py-md-5">
    <nav class="navbar navbar-expand-lg navbar-dark dark flex-md-column p-0">
        <ul class="w-100">
            <h4>Catégories</h4>
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('wild_index') }}" title="Toutes les séries">
                    Tout voir
                </a>
            </li>
            {% for category in categories %}
                <li class="nav-item">
                    <a class="nav-link"
                       href="{{ path('wild_category', { 'categoryName': category.name }) }}"
                       title="Catégorie {{ category.name }}">
                        {{ category.name }}
                    </a>
                </li>
            {% endfor %}
            <hr>
            <li class="nav-item">
                <a class="nav-link"
                   href="{{ path('wild_all_actors') }}"
                   title="Tous les acteurs">
                    Les acteurs
                </a>
            </li>
        </ul>
    </nav>
</div>
