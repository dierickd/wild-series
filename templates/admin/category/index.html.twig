{% extends 'base.html.twig' %}

{% block title %}Catégories | admin{% endblock %}

{% block body %}
    <div class="container">
        <h1>Liste des catégories</h1>

        <a class="btn btn-sm btn-outline-dark my-4" href="{{ path('admin') }}">Accueil admin</a>
        <a class="btn btn-sm btn-dark" href="{{ path('category_new') }}">Nouvelle catégorie</a>

        <table class="table table-hover">
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for category in categories %}
                <tr>
                    <td>{{ category.id }}</td>
                    <td>{{ category.name }}</td>
                    <td>
                        <a class="btn btn-sm btn-outline-dark" href="{{ path('category_show', {'id': category.id}) }}">Afficher</a>
                        <a class="btn btn-sm btn-dark" href="{{ path('category_edit', {'id': category.id}) }}">Editer</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>
{% endblock %}
