{% extends 'base.html.twig' %}

{% block title %}Editer un programme | Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1>Editer</h1>

        {{ include('admin/program/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-sm btn-secondary" href="{{ path('program_index') }}">Retour à la liste</a>

        {{ include('admin/program/_delete_form.html.twig') }}
    </div>
{% endblock %}
