{% extends 'base.html.twig' %}

{% block title %}Programmes | Admin{% endblock %}

{% block body %}
    <div class="container">
        <h1>Visualiser</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ program.title }}</td>
            </tr>
            <tr>
                <th>Summary</th>
                <td>{{ program.summary }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ program.poster }}</td>
            </tr>
            </tbody>
        </table>

        <a class="btn btn-sm btn-secondary" href="{{ path('program_index') }}">Retour à la liste</a>

        <a class="btn btn-sm btn-dark" href="{{ path('program_edit', {'id': program.id}) }}">Editer</a>

        {{ include('admin/program/_delete_form.html.twig') }}
    </div>
{% endblock %}
