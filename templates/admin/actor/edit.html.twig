{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Actor</h1>
        <a class="btn btn-sm btn-dark my-4" href="{{ path('actor_index') }}">Retour à la liste</a>

        {{ include('admin/actor/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        {{ include('admin/actor/_delete_form.html.twig') }}
    </div>
{% endblock %}
